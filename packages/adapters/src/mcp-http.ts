import type { AdapterContext, HealthResult } from './contracts.js';
export interface McpServerConfig{endpoint:string;headers?:Record<string,string>;}
export class McpHttpClient{constructor(private readonly cfg:McpServerConfig){}private async call(method:string,params:unknown,ctx:AdapterContext){const r=await fetch(this.cfg.endpoint,{method:'POST',headers:{'content-type':'application/json',...this.cfg.headers},body:JSON.stringify({jsonrpc:'2.0',id:crypto.randomUUID(),method,params}),signal:ctx.signal});if(!r.ok)throw new Error(`MCP_HTTP_${r.status}`);const body=await r.json() as {result?:unknown;error?:{message?:string}};if(body.error)throw new Error(body.error.message||'MCP_ERROR');return body.result;}async listTools(ctx:AdapterContext){return this.call('tools/list',{},ctx);}async callTool(name:string,args:unknown,ctx:AdapterContext){return this.call('tools/call',{name,arguments:args},ctx);}async health(ctx:AdapterContext):Promise<HealthResult>{try{await this.call('ping',{},ctx);return{status:'healthy'};}catch(e){return{status:'unhealthy',details:{error:e instanceof Error?e.message:'unknown'}};}}}
