import type { FastifyInstance } from 'fastify';
import { PostgresNotificationRepository, createPool } from '@axsd/storage';
type Notification={id:string;userId:string;kind:string;message:string;read:boolean;createdAt:string};
const memory:Notification[]=[];
export function registerNotificationRoutes(app:FastifyInstance){
 app.get('/api/v1/notifications',async(request,reply)=>{const userId=request.principal?.id;if(!userId)return reply.status(401).send({error:{code:'AUTH_ERR',message:'Authentication required'}});if(process.env.DATABASE_URL){const pool=createPool();try{return{data:await new PostgresNotificationRepository(pool).list(userId)}}finally{await pool.end();}}return{data:memory.filter(n=>n.userId===userId)}});
 app.post<{Body:{kind:string;message:string}}>('/api/v1/notifications',async(request,reply)=>{const userId=request.principal?.id;if(!userId)return reply.status(401).send({error:{code:'AUTH_ERR',message:'Authentication required'}});const body=request.body??{};if(typeof body.kind!=='string'||typeof body.message!=='string'||!body.kind.trim()||!body.message.trim())return reply.status(400).send({error:{code:'VALIDATION_ERR',message:'kind and message are required'}});const n={id:crypto.randomUUID(),userId,kind:body.kind.trim(),message:body.message.trim(),read:false,createdAt:new Date().toISOString()};if(process.env.DATABASE_URL){const pool=createPool();try{await new PostgresNotificationRepository(pool).create({id:n.id,userId:n.userId,type:n.kind,title:n.kind,body:n.message});return reply.status(201).send({data:n})}finally{await pool.end()}}memory.push(n);return reply.status(201).send({data:n})});
}
