import {createHash} from 'node:crypto';
export interface AuditChainItem{eventId:string;actorId:string;action:string;executionId?:string;resourceId?:string;metadata:Record<string,unknown>;integrityHash:string;previousHash:string;}
export function verifyAuditChain(items:AuditChainItem[]):{valid:boolean;brokenAt?:number}{let previous='';for(let i=0;i<items.length;i++){const x=items[i];if(x.previousHash!==previous)return {valid:false,brokenAt:i};const payload=JSON.stringify({previous,x:eventWithoutHash(x)});const expected=createHash('sha256').update(payload).digest('hex');if(expected!==x.integrityHash)return {valid:false,brokenAt:i};previous=x.integrityHash;}return {valid:true};}
function eventWithoutHash(x:AuditChainItem){return {eventId:x.eventId,actorId:x.actorId,action:x.action,executionId:x.executionId,resourceId:x.resourceId,metadata:x.metadata};}
