import type { Pool } from 'pg';
export interface ApprovalRecord{id:string;executionId:string;actorId:string;action:string;risk:string;argumentSummary:string;estimatedCost?:number;decision?:string;decidedBy?:string;}
export class PostgresApprovalRepository{constructor(private readonly pool:Pool){}async create(a:ApprovalRecord){await this.pool.query(`INSERT INTO approvals(id,execution_id,actor_id,action,risk,argument_summary,estimated_cost) VALUES($1,$2,$3,$4,$5,$6,$7)`,[a.id,a.executionId,a.actorId,a.action,a.risk,a.argumentSummary,a.estimatedCost??null]);}async listPending(){const {rows}=await this.pool.query(`SELECT id,execution_id AS "executionId",actor_id AS "actorId",action,risk,argument_summary AS "argumentSummary",estimated_cost AS "estimatedCost",decision,decided_by AS "decidedBy" FROM approvals WHERE decision IS NULL ORDER BY created_at ASC`);return rows as ApprovalRecord[]}async decide(id:string,decision:string,userId:string){const r=await this.pool.query(`UPDATE approvals SET decision=$2,decided_by=$3,decided_at=NOW() WHERE id=$1 AND decision IS NULL`,[id,decision,userId]);if(r.rowCount!==1)throw new Error('APPROVAL_NOT_PENDING');}}
