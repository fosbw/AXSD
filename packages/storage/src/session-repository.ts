import type { Pool } from 'pg';
export interface SessionRecord{id:string;userId:string;projectId?:string;status:'active'|'paused'|'stopped'|'archived';createdAt:string;}
export class PostgresSessionRepository{constructor(private readonly pool:Pool){}async create(v:SessionRecord){await this.pool.query(`INSERT INTO sessions(id,user_id,project_id,status) VALUES($1,$2,$3,$4)`,[v.id,v.userId,v.projectId??null,v.status]);return v;}async get(id:string,userId:string){const {rows}=await this.pool.query(`SELECT id,user_id AS "userId",project_id AS "projectId",status,created_at AS "createdAt" FROM sessions WHERE id=$1 AND user_id=$2`,[id,userId]);return rows[0]??null;}async list(userId:string){const {rows}=await this.pool.query(`SELECT id,user_id AS "userId",project_id AS "projectId",status,created_at AS "createdAt" FROM sessions WHERE user_id=$1 ORDER BY created_at DESC`,[userId]);return rows as SessionRecord[]}async update(id:string,userId:string,status:SessionRecord['status']){const {rows}=await this.pool.query(`UPDATE sessions SET status=$3,updated_at=NOW() WHERE id=$1 AND user_id=$2 RETURNING id,user_id AS "userId",project_id AS "projectId",status,created_at AS "createdAt"`,[id,userId,status]);return rows[0]??null;}}
export class InMemorySessionRepository{private readonly items=new Map<string,SessionRecord>();create(v:SessionRecord){this.items.set(v.id,v);return v;}get(id:string){return this.items.get(id)??null;}list(){return[...this.items.values()];}update(id:string,status:SessionRecord['status']){const item=this.items.get(id);if(!item)return null;const updated={...item,status};this.items.set(id,updated);return updated;}}
